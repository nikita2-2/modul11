#ifndef TREE_H
#define TREE_H

#include <iostream>

enum TreeType { OAK, MAPLE, BIRCH };

class Tree {
public:
    Tree(TreeType type); // ???????????

    // ??????????? ????? wind
    virtual void wind() const = 0;

    // ???????? ??? ??????
    TreeType getType() const;

    // ???????? ???????
    static int getCount(TreeType type);

protected:
    TreeType treeType;
    static int oakCount;
    static int mapleCount;
    static int birchCount;
};

#endif // TREE_H
